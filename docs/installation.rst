============
Installation
============

At the command line::

    $ easy_install django-behaviors

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-behaviors
    $ pip install django-behaviors
